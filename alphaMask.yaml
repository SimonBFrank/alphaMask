download: bash ./../alphaMask/data/export_data.sh

train: data/alphaMask/images/training
val: data/alphaMask/images/validation

# number of classes
nc: 3

# class names
names: ['without_mask', 'incorrect_mask', 'correct_mask']
