#download: bash ./../alphaMask/data/export_data.sh

train: data/data/images/training
val: data/data/images/validation

# number of classes
nc: 3

# class names
names: ['without_mask', 'incorrect_mask', 'correct_mask']
